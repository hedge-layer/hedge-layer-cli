# Optional shell environment variables. The CLI does not load .env files.
# Flags override environment variables; environment overrides saved config.

# HL_API_URL=https://hedgelayer.ai
# HL_TOKEN=<api-token>
