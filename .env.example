# Hedge Layer CLI environment variables (all optional)
# Copy this file to .env and fill in values as needed.

# Override the default API base URL (default: https://hedgelayer.ai)
# HL_API_URL=https://hedgelayer.ai

# Provide an API token instead of using `hl auth login`
# HL_TOKEN=hl_your_token_here
